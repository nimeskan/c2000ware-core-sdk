% let Common   = system.getScript("/driverlib/Common.js");
{
% let keys = Object.keys(system.modules).sort();
% for (let i = 0; i < keys.length; i++) {
%	let mod = system.modules[keys[i]];
%	let end = ","
%	if (i == (keys.length - 1)) {
%	end = ""
%	}
	"`keys[i]`" : `JSON.stringify(mod.$configByName, null, 4)``end`
% }
}